{% extends 'base.html.twig' %}

{% trans_default_domain 'general' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}Edit Accounts{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ 'account.edit.title'|trans }}</h1>

        {{ include('accounts/_form.html.twig', {'button_label': 'account.library.edit'|trans}) }}

        <a class="btn btn-secondary" href="{{ path('index_librarie', {'id': account.id}) }}">
            {{ 'account.edit.return_account'|trans }}
        </a>

        {{ include('accounts/_delete_form.html.twig') }}
    </div>
{% endblock %}
